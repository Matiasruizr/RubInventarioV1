<%= form_with(model: materia_prima, local: true) do |form| %>
  <% if materia_prima.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(materia_prima.errors.count, "error") %> prohibited this materia_prima from being saved:</h2>

      <ul>
      <% materia_prima.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id_mp %>
    <%= form.text_field :id_mp %>
  </div>

  <div class="field">
    <%= form.label :color %>
    <%= form.text_field :color %>
  </div>

  <div class="field">
    <%= form.label :cantidad %>
    <%= form.number_field :cantidad %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
